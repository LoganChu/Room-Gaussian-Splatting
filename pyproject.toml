[project]
name = "room-gaussian-splatting"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pillow>=12.3.0",
    "pyarrow>=25.0.1",
    "pycolmap>=4.2",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
